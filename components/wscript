def build( bld ):
	subdirs = [ 'CallComparerComponent', 'HaplotypeFrequencyComponent', 'SNPSummaryComponent', 'RelatednessComponent' ]
        for subdir in subdirs:
                bld.add_subdirs( subdir )

